// (removed) Inventory/culture status helpers — SpliceVar is a pure analysis
// and output tool; lab-management features were dropped. File intentionally
// left empty; safe to delete on a machine where the mount allows it.
export {};
