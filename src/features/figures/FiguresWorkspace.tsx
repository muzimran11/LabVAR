// Figures / RNA-seq workspace was removed (volcano plot, gene lookup, pathway
// enrichment). Parked out of the app per product direction. This stub exists
// only because the mounted filesystem blocks deletion; nothing imports it.
export {};
